// No ES5, não existia suporte para constantes. A partir do ES6 é possível declarar constantes. Constantes são variáveis que não podem ser alteradas após inicializadas.

const a = 1;

// a = 2; // Error

console.log(a);