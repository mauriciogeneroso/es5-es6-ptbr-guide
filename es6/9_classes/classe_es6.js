class Triangulo {

    constructor(base, altura) {
        this.base = base;
        this.altura = altura;
    }
}

let triangulo = new Triangulo(10, 20);
console.log(triangulo.base, triangulo.altura);